import Lif from "../../../Commons/Lif";

const Volumes = () => {
  return (
    <section data-auto-animate>
      <h2>{"Volumes"}</h2>
      <ul>
        <Lif>
          Interacting with <strong>container</strong> filesystem
        </Lif>
        <Lif>Persisting data</Lif>
      </ul>
    </section>
  );
};

export default Volumes;
